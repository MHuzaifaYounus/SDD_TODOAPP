# migrations/001_add_reminder_fields.py

# This is a dummy migration script to represent the database schema update.
# In a real application, this would be handled by a migration tool like Alembic.

print("Applying migration: Add reminder and recurrence fields to the Task table.")

# Example SQL (for demonstration purposes):
# ALTER TABLE tasks ADD COLUMN reminderTime TIMESTAMP;
# ALTER TABLE tasks ADD COLUMN isRecurring BOOLEAN;
# ALTER TABLE tasks ADD COLUMN recurrencePattern VARCHAR(255);
# ALTER TABLE tasks ADD COLUMN lastRecurrenceDate TIMESTAMP;

print("Migration complete.")
